# Default-project-settings

Gulp assembly.
Packages installed:
  - view changes;
  - Minimization of js and css;
  - Adding sprites;
  - Fileinclude (html);
  - Convert scss to css;
  - Added auto prefixes;
